<!-- Blogs -->
<section id="blogs">
    <h2>Blog</h2>

    <div class="blogs-grid">
        <!-- Blog Card -->
        <a
            href="/posts/my-first-post"
            class="blog-card-link"
            aria-label="Read blog: Why Every Developer Should Deploy on a VPS at Least Once"
        >
            <article class="blog-card">
                <div class="blog-header">
                    <h3 class="blog-title">
                        Why Every Developer Should Deploy on a VPS at Least Once
                    </h3>
                    <span class="blog-date">Dec 25, 2025</span>
                </div>

                <p class="blog-description">
                    A practical breakdown of why deploying a project on a VPS
                    builds real-world engineering skills beyond just writing
                    code.
                </p>

                <span class="blog-link"> Read more → </span>
            </article>
        </a>
    </div>
</section>

<style>
    .blogs-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
    }

    .blog-card-link {
        text-decoration: none;
        color: inherit;
        display: block;
        transition: transform 0.2s ease;
    }

    .blog-card-link:hover .blog-card {
        transform: translateY(-4px);
        border-color: #00ff90;
    }

    .blog-card-link:hover .blog-link {
        transform: translateX(4px);
        opacity: 0.9;
    }

    .blog-card {
        display: flex;
        flex-direction: column;
        gap: 0.8rem;
        background: #1a1c25;
        border: 1px solid #333;
        border-radius: 12px;
        padding: 1.4rem;
        transition:
            transform 0.2s ease,
            border-color 0.2s ease;
    }

    .blog-card:hover {
        transform: translateY(-4px);
        border-color: #00ff90;
    }

    /* Header */
    .blog-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 1rem;
    }

    .blog-title {
        font-size: 1.15rem;
        font-weight: 700;
        line-height: 1.3;
        color: #fff;
    }

    .blog-date {
        font-size: 0.9rem;
        font-weight: 700;
        color: #00ff90;
        letter-spacing: 0.3px;
        white-space: nowrap;
    }

    /* Description */
    .blog-description {
        font-size: 0.95rem;
        opacity: 0.9;
        line-height: 1.5;
    }

    /* Link */
    .blog-link {
        margin-top: auto;
        font-weight: 600;
        font-size: 0.95rem;
        color: #00ff90;
        text-decoration: none;
        width: fit-content;
        transition:
            opacity 0.15s ease,
            transform 0.15s ease;
    }

    .blog-link:hover {
        opacity: 0.9;
        transform: translateX(4px);
    }

    /* Responsive */
    @media (max-width: 600px) {
        .blogs-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
